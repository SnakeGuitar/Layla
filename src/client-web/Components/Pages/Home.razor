@page "/"
@page "/home"
@using client_web.Components.Components
<PageTitle>Home</PageTitle>

@* Header *@
<header class="max-w-5xl mx-auto mb-12">
    <h2 class="text-amber-500 text-xs font-bold tracking-[0.3em] uppercase mb-3">Projects</h2>
    <h1 class="text-stone-100 font-serif text-4xl md:text-5xl font-light tracking-tight">
        Public Library
    </h1>
    <div class="mt-4 w-16 h-px bg-amber-500"></div>
</header>

@* Feed Grid *@
@if (projects == null || !projects.Any()){
    <p class="text-stone-500 text-sm">No projects available.</p>
}
else
{
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-px">
        @foreach (ProjectViewModel project in projects)
        {
            <div>
                <ProjectCard Id="@project.Id" Title="@project.Title"
                                Synopsis="@project.Synopsis"
                                LiteraryGenre="@project.LiteraryGenre"
                                CoverImageUrl="@project.CoverImageUrl"
                                UpdatedAt="@project.UpdatedAt"
                                OnCardClick="HandleCardClick" />
            </div>
        }
    </div>
}

@code {
    private List<ProjectViewModel>? projects;

    protected override void OnInitialized()
    {
        // Replace with your actual data service call, e.g.:
        // projects = await ProjectService.GetAllAsync();

        projects = new List<ProjectViewModel>
        {
            new ProjectViewModel() {
                Id = Guid.NewGuid(),
                Title = "The Last Garden of Dreams",
                Synopsis = "A story about memory, dreams, and the secret gardens we keep deep within our minds.",
                LiteraryGenre = "Fantasy",
                CoverImageUrl = null,
                UpdatedAt = DateTime.Now.AddDays(-3)
            },
            new ProjectViewModel() {
                Id = Guid.NewGuid(),
                Title = "Letters from the End of the World",
                Synopsis = "An intimate epistolary that traces the last days of a civilization through the letters of those who lived it.",
                LiteraryGenre = "Science Fiction",
                CoverImageUrl = null,
                UpdatedAt = DateTime.Now.AddDays(-10)
            },
            new ProjectViewModel() {
                Id = Guid.NewGuid(),
                Title = "The City of Broken Mirrors",
                Synopsis = "A detective without a past investigates disappearances in a city where mirrors show what could have been.",
                LiteraryGenre = "Noir",
                CoverImageUrl = null,
                UpdatedAt = DateTime.Now.AddDays(-1)
            },
        };
    }

    private void HandleCardClick(Guid projectId)
    {
        // Navigate to project detail, e.g.:
        // NavigationManager.NavigateTo($"/projects/{projectId}");
        Console.WriteLine($"Clicked project: {projectId}");
    }

    private class ProjectViewModel
    {
        public Guid Id { get; set; }
        public string Title { get; set; } = string.Empty;
        public string Synopsis { get; set; } = string.Empty;
        public string LiteraryGenre { get; set; } = string.Empty;
        public string? CoverImageUrl { get; set; }
        public DateTime UpdatedAt { get; set; }
    }
}
